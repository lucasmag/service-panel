version: '3'
services:
    database:
        container_name: servicepanel-db
        image: postgres:latest
        restart: always
        environment:
            - POSTGRES_USER=postgres
            - POSTGRES_PASSWORD=postgres
            - POSTGRES_DB=postgres
        ports:
            - 5432:5432
        volumes:
            - ./pgdata:/var/lib/postgresql/data
